#!/usr/bin/env bash
# JankyBorders — active window outline (Tokyo Night, matches sketchybar)

options=(
  style=round
  width=6.0
  hidpi=off
  active_color=0xff7aa2f7
  inactive_color=0xff414868
)

borders "${options[@]}"
